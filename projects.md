# 一些主要的工作项目


## GPU 召回

召回突破双塔，与算法团队合作探索 GPU 召回，上线“二向箔”召回索引服务，使用 NN 模型打分 + hnsw 索引建模，允许在模型内使用交叉结构；引入 Beam Search 检索模式，在检索量 / 候选量 < 2% 的情况下，召回覆盖率达到暴力 GPU 召回的 90%+，在电商场景取得显著的 GMV 收益

1. 主要谈一谈图外优化手段和图内优化的参与度
